-- Generic monitor fallback for portable installs.
-- Users can replace this with their own monitor layout after installation.

hl.monitor({
	output = "",
	mode = "preferred",
	position = "auto",
	scale = 1,
})
